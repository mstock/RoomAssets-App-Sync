#!/usr/bin/env perl

# ABSTRACT: Synchronize room assets from Pretalx to Nextcloud

# PODNAME: room-assets-sync

use strict;
use warnings;

use FindBin;
use lib "$FindBin::Bin/../lib";

use RoomAssets::App::Sync;

exit RoomAssets::App::Sync->new_with_options()->run();
